# Backend environment variables

# JWT Secret Key - MUST change in production
SECRET_KEY=change-this-in-production-use-strong-random-string

# Google OAuth Client ID (required to enforce audience validation)
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# Database URL (PostgreSQL for production, SQLite for development)
# Development: sqlite:///./sql_app.db (default if not set)
# Production (Render): Will be auto-populated from DATABASE_URL
DATABASE_URL=

# CORS Origins (comma-separated, for production)
# Example: https://your-app.web.app,https://your-app.firebaseapp.com
CORS_ORIGINS=*
